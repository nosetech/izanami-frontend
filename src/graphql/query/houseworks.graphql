query listHouseWorks($familyId: ID!, $sort: HouseworkSortInput) {
  houseworks(familyId: $familyId, sort: $sort) {
    edges {
      cursor
      node {
        id
        title
        description
        category
        schedule
        suggestedBy {
          id
          name
        }
        point
        committed
        createdAt
        updatedAt
      }
    }
    pageInfo {
      startCursor
      endCursor
      hasNextPage
      hasPreviousPage
    }
    totalCount
  }
}
