query listHouseWorks($familyId: ID!) {
  houseworks(familyId: $familyId) {
    edges {
      cursor
      node {
        id
        title
        description
        category
        schedule
        suggestedBy {
          id
          name
        }
        point
        committed
        createdAt
        updatedAt
      }
    }
    pageInfo {
      startCursor
      endCursor
      hasNextPage
      hasPreviousPage
    }
    totalCount
  }
}
